// Stub used by next.config.mjs to replace Obsidian plugin files during local builds.
// Obsidian's .obsidian directory only exists locally; it is gitignored in CI.
export default function Page() { return null; }
